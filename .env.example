# Copy to .env and adjust. Never commit .env.
SENSECAST_CONFIG=            # empty = full profile, or "small"
SENSECAST_AUTH_MODE=off      # off | header (header = identity from a trusted gateway)
SENSECAST_RATE_LIMIT_PER_MIN=600
API_URL=http://localhost:8000
